name: Atay-Test

services:
  db:
    image: postgres:18.3-bookworm
    volumes:
      - ./data/db_data:/var/lib/postgresql
    env_file:
      - .env
    ports:
      - 5432:5432

  backend:
    build: .
    ports:
      - 8085:8085
    depends_on:
      - db
    env_file:
      - .env
